/*
 * This file is part of RPorta. For licensing and copyright information
 * please see the file COPYING in the root directory of this
 * distribution or contact <robin.nunkesser@tu-dortmund.de>.
 */

//#ifndef _POI_H
//#define _POI_H

#include "porta.h"
#include "arith.h"
#include "stdlib.h"	
#include "common.h"
#include "printMethods.h"
#include "valid.h"

//SEXP rConeReduction(SEXP pRSourceMatrix, SEXP pdimRMatrix, SEXP pRPointsToTest, SEXP pRdimTestPoints, SEXP pfailurePoints);

RAT * translation;  
/* stores the components of the translation vector with which the cone 
   will be translated to the center of the system
*/ 

//SEXP rConeReduction(SEXP pRSourceMatrix, SEXP pdimRMatrix, SEXP pRPointsToTest, SEXP pRdimTestPoints, SEXP pfailurePoints);

//#endif
